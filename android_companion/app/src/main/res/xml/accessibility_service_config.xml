<?xml version="1.0" encoding="utf-8"?>
<!-- Hermus accessibility config: observe + disable/tap only. No private data is
     collected; the service reports the current window hierarchy to the app. -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:notificationTimeout="100"
    android:canRetrieveWindowContent="true"
    android:canPerformGestures="true"
    android:canRequestFilterKeyEvents="false"
    android:description="@string/accessibility_desc"
    android:settingsActivity="com.hermus.companion.ui.PairingActivity" />
