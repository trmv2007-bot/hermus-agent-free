# Hermus Agent Free — development dependencies (lint, types, coverage, hooks).
# Production/runtime installs use requirements.txt only.
-r requirements.txt

ruff>=0.14.0
mypy>=1.0.0
pytest-cov>=5.0.0
pre-commit>=3.0.0
