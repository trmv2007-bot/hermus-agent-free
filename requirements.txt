# Hermus Agent Free - 100% Free Stack, No Paid APIs - Now with Browser, Vision, Voice, Backends, Trajectory

# Core
pydantic>=2.0.0
python-dotenv>=1.0.0

# LLM Free Providers
# Ollama - local free LLMs, no API key, runs on laptop/$5 VPS
requests>=2.31.0
# Groq free tier (optional, free key) - fast cloud
groq>=0.4.0
# HuggingFace free inference (optional, free token)
huggingface_hub>=0.23.0

# Memory + Token Counting - SQLite FTS5 built-in + tiktoken free
# sqlite3 is builtin Python
tiktoken>=0.5.0

# Tools - Free, no API keys
duckduckgo-search>=5.0.0
# File tools builtin
# Shell builtin subprocess

# Browser Automation Playwright Free - No API key
playwright>=1.40.0

# Vision LLaVA via Ollama Free - No API key, local vision
# Uses Ollama LLaVA via requests, no extra pip dep, but for image handling:
Pillow>=10.0.0
# Ollama already required, llava model pulled via ollama pull llava:7b (free)
# Conventional MP4/WebM screen recording (bundled free FFmpeg binary fallback)
imageio-ffmpeg>=0.5.1

# Voice Memo Transcription faster-whisper Free - Local Whisper, no API key
faster-whisper>=0.9.0

# Optional advanced speech/avatar integrations
# OmniVoice is intentionally not a hard dependency. Install separately if you
# want advanced multilingual TTS / voice cloning / voice design support:
#   pip install omnivoice torch soundfile
# HeyGem-style avatar support uses the optional local HTTP connector in
# core/avatar.py and targets an already-running localhost stack.

# Backends - Seven terminal backends free
# local always available
# docker - requires docker binary free
# ssh - requires ssh binary free, paramiko optional free
paramiko>=3.0.0
# singularity - HPC free
# modal - serverless persistence free tier
modal>=0.60.0
# daytona - serverless free tier
# vercel - vercel sandbox free tier

# Gateway - Free Bot APIs
fastapi>=0.104.0
python-multipart>=0.0.9  # multipart form parsing (dashboard file attachments)
psutil>=5.9.0  # live JARVIS process/system telemetry
uvicorn[standard]>=0.24.0
python-telegram-bot>=20.0
discord.py>=2.3.0
# Slack webhook via requests (free)
# WhatsApp/Signal via free bridges (optional)

# Scheduler - Free
APScheduler>=3.10.0
# Natural language cron parsing - free via LLM, no paid parser

# TUI - Free
prompt_toolkit>=3.0.41
rich>=13.0.0

# Subagents + Multi-Key + Multi-AI - Free (multiprocessing builtin)
# No extra deps

# Trajectory Batch Generation + Compression - Free
# No extra dep, uses core modules

# Custom API - Free
# Uses requests already required

# Dev
pytest>=8.0.0
httpx>=0.24.0

