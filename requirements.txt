# Hermus Agent Free — canonical required runtime dependencies
#
# setup.sh/bootstrap.py install this file with the interpreter in .venv. Keep
# package names here (rather than duplicating them in shell/Python lists). The
# optional integrations live in requirements-optional.txt and are installed
# independently so one unavailable platform integration cannot hide a core
# installation failure.

# Core configuration, HTTP, token accounting
pydantic>=2.0.0
python-dotenv>=1.0.0
requests>=2.31.0
tiktoken>=0.5.0

# Search and web/browser runtime used by the enabled web subsystem
ddgs>=9.0.0
playwright>=1.40.0
scrapling[fetchers]>=0.3.10,<0.5

# Vision, document/media support used by current tools
Pillow>=10.0.0
imageio-ffmpeg>=0.5.1
feedparser>=6.0.0
pypdf>=4.0.0
PyYAML>=6.0.0

# Voice input runtime. Model weights are downloaded only when the user selects
# a model; setup never downloads weights or records audio.
faster-whisper>=0.9.0

# Gateway, queue, telemetry and multipart uploads
fastapi>=0.104.0
python-multipart>=0.0.9
psutil>=5.9.0
uvicorn[standard]>=0.24.0
websockets>=12.0

# Scheduler and terminal UI
APScheduler>=3.10.0
prompt_toolkit>=3.0.41
rich>=13.0.0

# Verification/development dependencies used by the repository test suite
pytest>=8.0.0
httpx>=0.24.0
