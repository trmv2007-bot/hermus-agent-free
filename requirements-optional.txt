# Hermus Agent Free — optional integrations and accelerators
#
# bootstrap.py reads this file one requirement at a time. A failure is retained
# in the final capability report and never turns into a false success. These
# packages are useful only when the corresponding feature is configured or the
# host can provide the required service/device.

groq>=0.4.0
huggingface_hub>=0.23.0
paramiko>=3.0.0
modal>=0.60.0
python-telegram-bot>=20.0
discord.py>=2.3.0

# Real desktop control; the Computer agent remains safely usable in dry-run mode
# when the host has no display or window manager.
pyautogui>=0.9.54; platform_system=="Linux" or platform_system=="Darwin" or platform_system=="Windows"
pygetwindow>=0.0.9; platform_system=="Windows"

# TTS fallbacks and optional web output formats
pyttsx3>=2.90
soundfile>=0.12.1
redis>=5.0.0
sqlite-vec>=0.1.1
markdownify>=0.13.0
scrapling[ai]>=0.3.10,<0.5

# OmniVoice/OpenVINO are deliberately not installed automatically: their model
# runtimes and weights are large/device-specific. When requested, install them
# explicitly and re-run setup.sh --verify-only:
#   omnivoice
#   torch
#   openvino
